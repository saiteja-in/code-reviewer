{
  "version": 1,
  "description": "Golden cases for graph-mode context assembly and A/B review comparison.",
  "cases": [
    {
      "id": "cross-file-caller-impact",
      "description": "Changing UserService.find return type should surface handleUser caller outside the diff.",
      "repositoryId": "graph-fixture-repo",
      "prTitle": "Change UserService.find return type",
      "changedFiles": [
        {
          "filename": "fixtures/sample-service.ts",
          "status": "modified",
          "patch": "@@ -1,8 +1,8 @@\n export class UserService {\n-  find(id: string): string {\n+  find(id: string): number {\n     return id;\n   }"
        }
      ],
      "expectContext": {
        "mustIncludePaths": ["fixtures/user-controller.ts"],
        "mustIncludeRoles": ["impacted"]
      },
      "expectGraphReview": {
        "mustMentionExternalPath": "user-controller",
        "category": "bug",
        "minSeverity": "high"
      }
    },
    {
      "id": "signature-param-removed",
      "description": "Removing a required parameter from find() should flag callers that still pass it.",
      "repositoryId": "graph-fixture-repo",
      "prTitle": "Remove id parameter from UserService.find",
      "changedFiles": [
        {
          "filename": "fixtures/sample-service.ts",
          "status": "modified",
          "patch": "@@ -1,5 +1,5 @@\n export class UserService {\n-  find(id: string): string {\n+  find(): string {\n     return id;\n   }"
        }
      ],
      "expectContext": {
        "mustIncludePaths": ["fixtures/user-controller.ts"],
        "mustIncludeRoles": ["impacted"],
        "minSnippets": 1
      },
      "expectGraphReview": {
        "mustMentionExternalPath": "user-controller",
        "category": "bug",
        "minSeverity": "high"
      }
    },
    {
      "id": "definition-callee-contract",
      "description": "When changed controller code misuses UserService.find, definition context should support the finding.",
      "repositoryId": "graph-fixture-repo",
      "prTitle": "Call find with wrong arity in handleUser",
      "changedFiles": [
        {
          "filename": "fixtures/user-controller.ts",
          "status": "modified",
          "patch": "@@ -1,6 +1,6 @@\n export function handleUser(id: string): string {\n   const service = createUserService();\n-  return service.find(id);\n+  return service.find(id, true);\n }"
        }
      ],
      "expectContext": {
        "mustIncludePaths": ["fixtures/sample-service.ts"],
        "mustIncludeRoles": ["definition"]
      },
      "expectGraphReview": {
        "mustMentionExternalPath": "sample-service",
        "category": "bug",
        "minSeverity": "high"
      }
    }
  ]
}
